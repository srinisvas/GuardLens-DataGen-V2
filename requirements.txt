# GuardLens DataGen V2 — pip requirements
#
# Target system:
#   NVIDIA A100 80GB PCIe (Ampere, sm_80)
#   Driver R580 LTS (580.105.08+), CUDA runtime 13.0
#   Linux x86_64
#
# Install:
#   conda create -n dataset_gen python=3.11 -y
#   conda activate dataset_gen
#   pip install -r requirements.txt
#
# --- VERSION PINNING POLICY ---
#
# vLLM is pinned to a specific range because Qwen3.5 requires a recent
# vLLM with the qwen3 reasoning parser and default_chat_template_kwargs
# support. Older vLLM (<=0.20) does not have these.
#
# AFTER PHASE 1 PASSES with these pins, capture the exact resolved
# versions with:
#     pip freeze > requirements.lock.txt
# and treat requirements.lock.txt as the reproducible artifact for the
# paper. Do not leave `>=` ranges in the frozen environment.

# ---- Core HTTP ----
requests>=2.31.0

# ---- Model / tokenizer stack ----
# torch 2.11 has CUDA 13.0 as PyPI default (stable since March 2026)
torch>=2.11.0,<2.13.0
transformers>=4.50.0,<5.0.0
accelerate>=0.34.0
sentencepiece>=0.2.0
protobuf>=4.25.0
safetensors>=0.4.5

# ---- HF download ----
huggingface_hub>=0.24.0

# ---- vLLM ----
# 0.28+ has the Qwen3.5 native implementation and reasoning-parser controls.
# TODO after Phase 1: replace with exact pin from pip freeze.
vllm>=0.28.0,<0.30.0

# ---- Datasets ----
datasets>=2.19.0

# ---- Config ----
PyYAML>=6.0

# ---- Contamination check ----
datasketch>=1.6.4
numpy>=1.26.0
sentence-transformers>=3.0.0
tqdm>=4.66.0